name: Auto-resolve PR conflicts

on:
  workflow_run:
    workflows: ["Auto-update PR branches"]
    types: [completed]

permissions:
  pull-requests: write
  contents: read
  issues: write

jobs:
  resolve:
    uses: aramiworks/.github/.github/workflows/auto-resolve-conflicts.yml@main
    secrets: inherit
