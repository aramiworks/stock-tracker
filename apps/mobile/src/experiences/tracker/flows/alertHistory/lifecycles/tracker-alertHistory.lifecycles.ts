/**
 * Flow-level lifecycle scaffold. The browse container handles refetch-on-focus
 * via `containers/browse/lifecycles/tracker-alertHistory-browse.lifecycles.ts`.
 */
export const useTrackerAlertHistoryLifecycle = (): void => {
  // No flow-level effects yet — kept as a stub so the MCVL shape stays uniform.
};
