appId: so.arami.stocktracker.app
name: Sanity Check
tags:
  - baseline
  - smoke
---
# Minimal check: app launches and shows the sign-in screen
- launchApp
- extendedWaitUntil:
    visible:
      id: "auth-signIn-gmailOauth-screen"
    timeout: 10000
