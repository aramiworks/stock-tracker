---
"@stock-tracker/mobile": patch
---

Split `auth-signIn-gmailOauth` views into per-component_set files (signInButton, loadingState, errorState) to match the EFCV/MCVL sub-view-per-file pattern used in `tracker` containers.
