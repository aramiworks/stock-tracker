---
"@stock-tracker/mobile": patch
---

ci(chromatic): widen paths-filter to trigger on `.view.tsx` and `.views.tsx` source changes so Chromatic runs when view rendering changes, not only when story files do.
