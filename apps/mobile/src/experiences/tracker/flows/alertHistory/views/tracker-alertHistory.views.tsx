import { memo } from "react";

/**
 * Flow-level views slot. The browse container owns the actual screen — see
 * `containers/browse/views/tracker-alertHistory-browse.views.tsx`.
 */
export const TrackerAlertHistoryViews = memo(() => {
  return null;
});

TrackerAlertHistoryViews.displayName = "TrackerAlertHistoryViews";
