---
"@stock-tracker/mobile": patch
---

Define `stocktracker` 7-agent team in `.claude/agents/` and `.claude/teams/stocktracker/README.md` so future Claude Code sessions can spawn the team via `subagent_type: stocktracker-<role>` without re-deriving charters.
