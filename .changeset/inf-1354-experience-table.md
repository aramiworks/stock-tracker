---
"@stock-tracker/mobile": patch
---

Update CLAUDE.md experience table for Shengsho UX pivot: tracker flows are now `catalog` / `watchlist` / `alertHistory` (was `alerts` / `watchlist` / `history`).
