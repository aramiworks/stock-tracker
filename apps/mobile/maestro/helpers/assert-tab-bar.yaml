# Helper: Assert the bottom tab bar is visible with correct tabs
url: ${MAESTRO_APP_URL}
---
- assertVisible:
    id: "tab-alerts"
- assertVisible:
    id: "tab-watchlist"
- assertVisible:
    id: "tab-history"
